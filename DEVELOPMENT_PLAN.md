# План разработки GUI и улучшений для проекта "Умный Дом"

## 📋 Общая информация
Этот план разбит на этапы, которые можно выполнять последовательно. Каждый этап независим и может быть протестирован отдельно.

---

## ЭТАП 1: Подготовка и улучшение имитации устройств  
**Цель:** Сделать устройства более "живыми" с автоизменениями параметров

### СДЕЛАНО Шаг 1.1: Добавить автоизменение температуры в термостате  
**Файлы для изменения:**
- `src/devices/climate/thermostat.py`

**Что сделать:**
- Добавить метод `_simulate_temperature()` который будет медленно изменять температуру
- Добавить фоновый поток для симуляции (или вызывать в `check_device_changes`)
- Температура должна плавно изменяться в диапазоне 18-26°C когда устройство включено

### СДЕЛАНО Шаг 1.2: Добавить автоизменение яркости в умной лампе
**Файлы для изменения:**
- `src/devices/lighting/smart_light.py`

**Что сделать:**
- Добавить метод `_simulate_brightness()` для плавного изменения яркости
- Яркость может изменяться случайно или по времени суток (имитация)
- Диапазон: 20-100% когда лампа включена

### СДЕЛАНО Шаг 1.3: Добавить симуляцию движения для камеры
**Файлы для изменения:**
- `src/devices/security/security_camera.py`

**Что сделать:**
- Добавить метод `_simulate_motion_detection()` 
- Периодически (раз в 30-60 секунд) "обнаруживать движение"
- При обнаружении отправлять событие через EventBus

### Шаг 1.4: Обновить DeviceManager для поддержки симуляций
**Файлы для изменения:**
- `src/devices/device_manager.py`

**Что сделать:**
- В методе `check_device_changes()` вызывать методы симуляции для каждого устройства
- Убедиться что симуляции работают только когда устройство включено

**Тестирование этапа 1:**
- Запустить консольный интерфейс
- Включить устройства и наблюдать автоизменения параметров
- Проверить что изменения отображаются в статусе

---

## ЭТАП 2: Создание простой базы данных (SQLite)
**Цель:** Сохранять историю состояний устройств и логи

### Шаг 2.1: Создать структуру базы данных
**Файлы для создания:**
- `src/database/__init__.py` (пустой файл)
- `src/database/db.py` - подключение к SQLite
- `src/database/models.py` - SQLAlchemy модели

**Что сделать:**
- Создать подключение к SQLite БД (`smart_home.db`)
- Создать модели:
  - `DeviceStateHistory` (device_id, state, timestamp, temperature, brightness и т.д.)
  - `SystemLog` (level, message, timestamp, source)
  - `NotificationRecord` (title, message, level, timestamp, read)

### Шаг 2.2: Обновить DeviceManager для сохранения в БД
**Файлы для изменения:**
- `src/devices/device_manager.py`

**Что сделать:**
- При изменении состояния устройства сохранять запись в `DeviceStateHistory`
- Импортировать модели из `database.models`
- Добавить метод `save_state_to_db(device_id, state_data)`

### Шаг 2.3: Обновить LoggingService для сохранения в БД
**Файлы для изменения:**
- `src/services/logging_service.py`

**Что сделать:**
- При логировании сохранять важные события в таблицу `SystemLog`
- Сохранять только INFO, WARNING, ERROR уровни

### Шаг 2.4: Обновить NotificationService для сохранения в БД
**Файлы для изменения:**
- `src/services/notification_service.py`

**Что сделать:**
- При создании уведомления сохранять в `NotificationRecord`
- При загрузке уведомлений читать из БД
- При пометке как прочитанное обновлять БД

### Шаг 2.5: Инициализация БД при старте
**Файлы для изменения:**
- `src/core/home_controller.py`

**Что сделать:**
- В `__init__` создать таблицы БД (если их нет)
- Использовать `db.create_tables()` или Alembic

**Тестирование этапа 2:**
- Запустить приложение
- Выполнить несколько действий с устройствами
- Проверить файл `smart_home.db` (должен появиться)
- Проверить что данные сохраняются в таблицах

---

## ЭТАП 3: Создание GUI интерфейса (Tkinter)
**Цель:** Графический интерфейс для управления системой

### Шаг 3.1: Создать базовую структуру GUI
**Файлы для создания:**
- `src/ui/widgets/__init__.py` (пустой файл)
- `src/ui/gui_interface.py` - главное окно

**Что сделать:**
- Создать класс `GUIInterface` с главным окном Tkinter
- Добавить базовую структуру: меню, панель статуса, область устройств
- Добавить метод `run()` для запуска GUI
- Добавить обновление интерфейса каждые 2 секунды

### Шаг 3.2: Создать виджет карточки устройства
**Файлы для создания:**
- `src/ui/widgets/device_card.py`

**Что сделать:**
- Создать класс `DeviceCard` (Frame в Tkinter)
- Отображать: название, состояние (вкл/выкл), онлайн статус
- Кнопки: Включить, Выключить, Переключить
- Для термостата: показывать температуру
- Для лампы: показывать яркость
- Для камеры: показывать статус записи

### Шаг 3.3: Интегрировать DeviceCard в главное окно
**Файлы для изменения:**
- `src/ui/gui_interface.py`

**Что сделать:**
- Создать карточки для всех устройств из DeviceManager
- Разместить их в сетке (grid) или используя pack
- Подключить кнопки к методам DeviceManager
- Обновлять карточки при изменении состояний

### Шаг 3.4: Добавить панель статуса системы
**Файлы для изменения:**
- `src/ui/gui_interface.py`

**Что сделать:**
- Верхняя панель: название системы, версия
- Статистика: количество устройств, онлайн, активных
- Счетчик непрочитанных уведомлений
- Кнопка "Обновить" для ручного обновления

### Шаг 3.5: Добавить панель уведомлений
**Файлы для изменения:**
- `src/ui/gui_interface.py` или создать отдельный виджет

**Что сделать:**
- Список последних уведомлений (Listbox или Text)
- Иконки по типу уведомления (info, warning, error)
- Кнопки: "Пометить все как прочитанные", "Очистить"
- Автообновление списка

### Шаг 3.6: Обновить main.py для выбора интерфейса
**Файлы для изменения:**
- `src/main.py`

**Что сделать:**
- Добавить аргументы командной строки или меню выбора
- Опции: `--gui` для GUI, `--console` для консоли (по умолчанию)
- Или интерактивный выбор при запуске

**Тестирование этапа 3:**
- Запустить `python src/main.py --gui`
- Проверить что все устройства отображаются
- Проверить что кнопки работают
- Проверить автообновление интерфейса

---

## ЭТАП 4: Улучшение GUI (дополнительные функции)
**Цель:** Сделать GUI более функциональным и удобным

### Шаг 4.1: Добавить окно настроек устройств
**Файлы для создания:**
- `src/ui/widgets/settings_dialog.py`

**Что сделать:**
- Диалог для изменения параметров устройства
- Для термостата: установка целевой температуры
- Для лампы: установка яркости (слайдер)
- Кнопки: Применить, Отмена

### Шаг 4.2: Добавить панель сценариев автоматизации
**Файлы для изменения:**
- `src/ui/gui_interface.py`

**Что сделать:**
- Кнопки для запуска готовых сценариев:
  - "Утро" - включить свет, поднять температуру
  - "Кино" - приглушить свет, выключить камеру
  - "Сон" - выключить все кроме камеры
- Интеграция с AutomationService

### Шаг 4.3: Добавить график истории температуры
**Файлы для создания:**
- `src/ui/widgets/temperature_chart.py`

**Что сделать:**
- Использовать matplotlib для отображения графика
- Читать данные из БД (DeviceStateHistory)
- Показывать последние 24 часа или выбранный период
- Кнопка "Обновить график"

**Файлы для изменения:**
- `requirements.txt` - добавить `matplotlib>=3.7.0`

### Шаг 4.4: Добавить меню и горячие клавиши
**Файлы для изменения:**
- `src/ui/gui_interface.py`

**Что сделать:**
- Меню: Файл (Выход), Настройки, Помощь
- Горячие клавиши: Ctrl+Q (выход), F5 (обновить)

**Тестирование этапа 4:**
- Проверить все новые функции
- Убедиться что графики отображаются корректно
- Проверить работу сценариев

---

## ЭТАП 5: Создание REST API (опционально, но рекомендуется)
**Цель:** API для демонстрации архитектуры и возможной интеграции

### Шаг 5.1: Создать базовую структуру API
**Файлы для создания:**
- `src/api/__init__.py` (пустой файл)
- `src/api/main.py` - FastAPI приложение
- `src/api/schemas.py` - Pydantic схемы для валидации

**Что сделать:**
- Создать FastAPI приложение
- Базовый эндпоинт `/` или `/health` для проверки работы
- Схемы: DeviceStatus, DeviceCommand, SystemStatus

### Шаг 5.2: Добавить эндпоинты для устройств
**Файлы для создания:**
- `src/api/routes/__init__.py` (пустой файл)
- `src/api/routes/devices.py`

**Что сделать:**
- `GET /api/devices` - список всех устройств
- `GET /api/devices/{device_id}` - статус конкретного устройства
- `POST /api/devices/{device_id}/command` - отправить команду
- `GET /api/devices/{device_id}/history` - история состояний

### Шаг 5.3: Добавить эндпоинты для системы
**Файлы для создания:**
- `src/api/routes/system.py`

**Что сделать:**
- `GET /api/system/status` - общий статус системы
- `GET /api/system/logs` - последние логи
- `GET /api/system/notifications` - уведомления

### Шаг 5.4: Добавить WebSocket для обновлений в реальном времени
**Файлы для изменения:**
- `src/api/main.py`

**Что сделать:**
- WebSocket эндпоинт `/ws` для подписки на события
- Отправлять обновления при изменении состояния устройств
- Интеграция с EventBus

### Шаг 5.5: Обновить main.py для запуска API
**Файлы для изменения:**
- `src/main.py`

**Что сделать:**
- Добавить опцию `--api` для запуска API сервера
- Или запускать API в фоне при запуске GUI
- Использовать uvicorn для запуска

**Файлы для изменения:**
- `requirements.txt` - добавить `fastapi>=0.104.1`, `uvicorn>=0.24.0`, `websockets>=12.0`

**Тестирование этапа 5:**
- Запустить API: `python src/main.py --api`
- Проверить эндпоинты через браузер или curl
- Проверить WebSocket соединение

---

## ЭТАП 6: Улучшение сценариев автоматизации
**Цель:** Более гибкая система автоматизации

### Шаг 6.1: Создать менеджер сценариев
**Файлы для создания:**
- `src/services/scenarios/__init__.py` (пустой файл)
- `src/services/scenarios/scenario_manager.py`

**Что сделать:**
- Класс `ScenarioManager` для управления сценариями
- Методы: `add_scenario()`, `run_scenario()`, `list_scenarios()`
- Сохранение сценариев в БД или JSON файл

### Шаг 6.2: Создать готовые сценарии
**Файлы для создания:**
- `src/services/scenarios/presets.py`

**Что сделать:**
- Предопределенные сценарии:
  - "Утро": свет 80%, температура 22°C, камера выкл
  - "Кино": свет 30%, температура 20°C, камера выкл
  - "Сон": свет выкл, температура 18°C, камера вкл
  - "Уход": все выкл, камера вкл

### Шаг 6.3: Интегрировать сценарии в GUI
**Файлы для изменения:**
- `src/ui/gui_interface.py`

**Что сделать:**
- Добавить панель "Сценарии" с кнопками
- При клике запускать соответствующий сценарий
- Показывать прогресс выполнения

**Тестирование этапа 6:**
- Запустить сценарии из GUI
- Проверить что все устройства изменяются корректно
- Проверить логи выполнения

---

## ЭТАП 7: Финальная полировка и документация
**Цель:** Подготовка к демонстрации

### Шаг 7.1: Обновить requirements.txt
**Файлы для изменения:**
- `requirements.txt`

**Что сделать:**
- Добавить все необходимые зависимости
- Указать версии для совместимости
- Добавить комментарии

### Шаг 7.2: Создать документацию
**Файлы для создания:**
- `docs/user_manual.md` - руководство пользователя
- `docs/developer_guide.md` - руководство разработчика
- `docs/architecture/gui_design.md` - описание GUI

**Что сделать:**
- Описать как запускать GUI
- Описать все функции интерфейса
- Скриншоты интерфейса (опционально)

### Шаг 7.3: Обновить README.md
**Файлы для изменения:**
- `README.md`

**Что сделать:**
- Добавить инструкции по установке зависимостей
- Описать как запускать разные режимы (GUI, консоль, API)
- Добавить примеры использования

### Шаг 7.4: Добавить обработку ошибок
**Файлы для изменения:**
- Все основные файлы GUI и API

**Что сделать:**
- Добавить try-except блоки
- Показывать понятные сообщения об ошибках пользователю
- Логировать ошибки

### Шаг 7.5: Финальное тестирование
**Что сделать:**
- Запустить все режимы работы
- Проверить все функции
- Убедиться что нет критических ошибок
- Подготовить демонстрацию

## ЭТАП 8: Добавление новых устройств (датчики и розетки)
**Цель:** Расширить набор устройств для более реалистичной системы

### Шаг 8.1: Создать датчик движения (MotionSensor)
**Файлы для создания:**
- `src/devices/sensors/__init__.py` (пустой файл)
- `src/devices/sensors/motion_sensor.py`

**Подробная реализация:**
1. Создать класс `MotionSensor` наследующийся от `BaseDevice`
2. В `__init__`:
   - `device_type = "sensor"`
   - `self.motion_detected = False` - флаг обнаружения движения
   - `self.sensitivity = 50` - чувствительность (0-100)
   - Добавить в `capabilities`: `["detect_motion", "set_sensitivity"]`
3. Реализовать методы:
   - `turn_on()` - активировать датчик
   - `turn_off()` - деактивировать датчик
   - `toggle()` - переключить состояние
   - `detect_motion()` - симуляция обнаружения движения (возвращает True/False)
   - `set_sensitivity(level)` - установить чувствительность
4. Добавить метод `_simulate_motion()`:
   - Периодически (каждые 30-90 секунд) случайно "обнаруживать движение"
   - Вероятность зависит от `sensitivity`
   - При обнаружении: `self.motion_detected = True`, отправить событие через `emit_event("motion_detected", {...})`
   - Через 5-10 секунд сбросить: `self.motion_detected = False`

**Файлы для изменения:**
- `src/devices/device_manager.py`:
  - В `_initialize_devices()` добавить: `self.add_device(MotionSensor("motion_sensor_1", "Датчик движения в коридоре"))`
  - В `check_device_changes()` вызывать `_simulate_motion()` для датчиков движения

### Шаг 8.2: Создать датчик температуры (TemperatureSensor)
**Файлы для создания:**
- `src/devices/sensors/temperature_sensor.py`

**Подробная реализация:**
1. Создать класс `TemperatureSensor` наследующийся от `BaseDevice`
2. В `__init__`:
   - `device_type = "sensor"`
   - `self.temperature = 22.0` - текущая температура
   - `self.humidity = 45.0` - влажность (опционально)
   - `self.location = "indoor"` - местоположение (indoor/outdoor)
3. Реализовать методы:
   - `turn_on()` - активировать датчик
   - `turn_off()` - деактивировать датчик
   - `toggle()` - переключить состояние
   - `get_temperature()` - получить текущую температуру
   - `get_humidity()` - получить влажность
4. Добавить метод `_simulate_reading()`:
   - Если датчик включен, изменять температуру плавно (18-26°C для indoor, -10 до 35°C для outdoor)
   - Изменение на 0.1-0.5°C каждые 10-30 секунд
   - Влажность: 30-70% с медленными изменениями
   - Отправлять событие при значительных изменениях (>1°C)

**Файлы для изменения:**
- `src/devices/device_manager.py`:
  - Добавить: `self.add_device(TemperatureSensor("temp_sensor_living", "Датчик температуры в гостиной", "indoor"))`
  - Добавить: `self.add_device(TemperatureSensor("temp_sensor_outdoor", "Датчик температуры на улице", "outdoor"))`

### Шаг 8.3: Создать датчик дыма (SmokeSensor)
**Файлы для создания:**
- `src/devices/sensors/smoke_sensor.py`

**Подробная реализация:**
1. Создать класс `SmokeSensor` наследующийся от `BaseDevice`
2. В `__init__`:
   - `device_type = "sensor"`
   - `self.smoke_level = 0` - уровень задымленности (0-100)
   - `self.alarm_threshold = 50` - порог срабатывания
   - `self.alarm_active = False` - флаг тревоги
3. Реализовать методы:
   - `turn_on()` - активировать датчик
   - `turn_off()` - деактивировать датчик
   - `toggle()` - переключить состояние
   - `get_smoke_level()` - получить уровень задымленности
   - `set_threshold(level)` - установить порог срабатывания
4. Добавить метод `_simulate_smoke()`:
   - Обычно `smoke_level = 0-5` (норма)
   - Редко (раз в 5-10 минут) случайно поднимать до 10-20 (имитация ложного срабатывания)
   - Очень редко (раз в 30-60 минут) поднимать выше порога (имитация реального дыма)
   - При превышении порога: `alarm_active = True`, отправить критическое уведомление через EventBus

**Файлы для изменения:**
- `src/devices/device_manager.py`:
  - Добавить: `self.add_device(SmokeSensor("smoke_sensor_kitchen", "Датчик дыма на кухне"))`

### Шаг 8.4: Создать умную розетку (SmartOutlet)
**Файлы для создания:**
- `src/devices/power/__init__.py` (пустой файл)
- `src/devices/power/smart_outlet.py`

**Подробная реализация:**
1. Создать класс `SmartOutlet` наследующийся от `BaseDevice`
2. В `__init__`:
   - `device_type = "outlet"`
   - `self.power_consumption = 0.0` - текущее энергопотребление (Вт)
   - `self.max_power = 2500.0` - максимальная мощность (Вт)
   - `self.energy_today = 0.0` - потребление за сегодня (кВт·ч)
   - `self.voltage = 220.0` - напряжение (В)
3. Реализовать методы:
   - `turn_on()` - включить розетку
   - `turn_off()` - выключить розетку
   - `toggle()` - переключить состояние
   - `get_power_consumption()` - получить текущее потребление
   - `get_energy_today()` - получить потребление за сегодня
   - `reset_energy_counter()` - сбросить счетчик
4. Добавить метод `_simulate_power()`:
   - Если розетка включена, генерировать случайное потребление (50-2000 Вт)
   - Обновлять `energy_today` на основе потребления
   - При выключении: `power_consumption = 0`

**Файлы для изменения:**
- `src/devices/device_manager.py`:
  - Добавить: `self.add_device(SmartOutlet("outlet_kitchen", "Розетка на кухне"))`
  - Добавить: `self.add_device(SmartOutlet("outlet_bedroom", "Розетка в спальне"))`

### Шаг 8.5: Обновить GUI для новых устройств
**Файлы для изменения:**
- `src/ui/widgets/device_card.py`

**Что сделать:**
- Для датчика движения: показывать статус "Обнаружено движение" / "Нет движения"
- Для датчика температуры: показывать температуру и влажность
- Для датчика дыма: показывать уровень задымленности и статус тревоги (красный индикатор при тревоге)
- Для розетки: показывать потребление мощности и энергопотребление за день

**Тестирование этапа 8:**
- Запустить систему и проверить что все новые устройства инициализируются
- Проверить симуляцию работы каждого устройства
- Проверить что события отправляются корректно
- Проверить отображение в GUI

---

## ЭТАП 9: Сервис расписаний и таймеров
**Цель:** Автоматическое выполнение действий по расписанию

### Шаг 9.1: Создать структуру сервиса расписаний
**Файлы для создания:**
- `src/services/scheduler/__init__.py` (пустой файл)
- `src/services/scheduler/schedule_service.py`

**Подробная реализация:**
1. Создать класс `ScheduleService`:
   ```python
   class ScheduleService:
       def __init__(self, home_controller):
           self.controller = home_controller
           self.schedules = []  # Список расписаний
           self.running = True
           self.thread = None
   ```

2. Создать класс `Schedule` для хранения одного расписания:
   ```python
   class Schedule:
       def __init__(self, schedule_id, name, time, days, actions):
           self.schedule_id = schedule_id
           self.name = name  # "Утро", "Вечер" и т.д.
           self.time = time  # "08:00" формат
           self.days = days  # [0,1,2,3,4,5,6] дни недели (0=понедельник)
           self.actions = actions  # [{"device_id": "...", "command": "on"}, ...]
           self.enabled = True
   ```

3. Методы `ScheduleService`:
   - `add_schedule(schedule)` - добавить расписание
   - `remove_schedule(schedule_id)` - удалить расписание
   - `get_schedules()` - получить все расписания
   - `enable_schedule(schedule_id)` - включить расписание
   - `disable_schedule(schedule_id)` - выключить расписание
   - `_check_schedules()` - проверка и выполнение расписаний (вызывается в цикле)

4. Метод `_check_schedules()`:
   - Каждую минуту проверять все активные расписания
   - Если текущее время совпадает с временем расписания И текущий день в списке дней
   - Выполнить все действия из `actions` через `DeviceManager`
   - Логировать выполнение

5. Запуск в отдельном потоке:
   ```python
   def start(self):
       self.thread = threading.Thread(target=self._run)
       self.thread.daemon = True
       self.thread.start()
   
   def _run(self):
       while self.running:
           self._check_schedules()
           time.sleep(60)  # Проверка каждую минуту
   ```

### Шаг 9.2: Добавить модели БД для расписаний
**Файлы для изменения:**
- `src/database/models.py`

**Что сделать:**
- Создать модель `Schedule`:
  - `id` (Primary Key)
  - `name` (String)
  - `time` (String) - время в формате "HH:MM"
  - `days` (String) - JSON массив дней недели
  - `actions` (String) - JSON массив действий
  - `enabled` (Boolean)
  - `created_at` (DateTime)

### Шаг 9.3: Сохранение и загрузка расписаний из БД
**Файлы для изменения:**
- `src/services/scheduler/schedule_service.py`

**Что сделать:**
- При инициализации загружать расписания из БД
- При добавлении/изменении расписания сохранять в БД
- При удалении удалять из БД

### Шаг 9.4: Создать готовые расписания
**Файлы для создания:**
- `src/services/scheduler/preset_schedules.py`

**Что сделать:**
- Функция `create_preset_schedules()` которая создает:
  - "Утро" - 07:00, пн-пт: включить свет (80%), термостат (22°C)
  - "Вечер" - 20:00, ежедневно: приглушить свет (40%)
  - "Ночь" - 23:00, ежедневно: выключить свет, понизить температуру (18°C)
  - "Выходной" - 09:00, сб-вс: включить все устройства

### Шаг 9.5: Интегрировать ScheduleService в HomeController
**Файлы для изменения:**
- `src/core/home_controller.py`

**Что сделать:**
- В `__init__` создать `self.schedule_service = ScheduleService(self)`
- В `start_system()` вызвать `self.schedule_service.start()`
- В `stop_system()` остановить сервис

### Шаг 9.6: Добавить GUI для управления расписаниями
**Файлы для создания:**
- `src/ui/widgets/schedule_panel.py`

**Подробная реализация:**
1. Создать панель с:
   - Список расписаний (Listbox)
   - Кнопки: "Добавить", "Изменить", "Удалить", "Включить/Выключить"
   - Поля для создания: название, время, дни недели (чекбоксы), действия

2. Диалог создания расписания:
   - Поле ввода названия
   - Поле времени (TimePicker или ввод "HH:MM")
   - Чекбоксы дней недели
   - Таблица действий: устройство + команда + параметры
   - Кнопки "Добавить действие", "Удалить действие"

3. Отображение расписаний:
   - Название, время, дни, статус (вкл/выкл)
   - Список действий

**Файлы для изменения:**
- `src/ui/gui_interface.py`:
  - Добавить вкладку "Расписания" или отдельное окно
  - Интегрировать `SchedulePanel`

**Тестирование этапа 9:**
- Создать тестовое расписание на ближайшую минуту
- Проверить что оно выполняется в нужное время
- Проверить сохранение в БД
- Проверить GUI для управления расписаниями

---

## ЭТАП 10: Сервис энергопотребления
**Цель:** Мониторинг и управление энергопотреблением системы

### Шаг 10.1: Создать сервис энергопотребления
**Файлы для создания:**
- `src/services/energy/__init__.py` (пустой файл)
- `src/services/energy/energy_service.py`

**Подробная реализация:**
1. Создать класс `EnergyService`:
   ```python
   class EnergyService:
       def __init__(self, home_controller):
           self.controller = home_controller
           self.daily_limit = 10.0  # кВт·ч в день
           self.monthly_limit = 300.0  # кВт·ч в месяц
   ```

2. Методы:
   - `get_total_consumption_today()` - общее потребление за сегодня
   - `get_total_consumption_month()` - общее потребление за месяц
   - `get_consumption_by_device(device_id, period)` - потребление конкретного устройства
   - `get_top_consumers(limit=5)` - топ потребителей энергии
   - `set_daily_limit(limit)` - установить дневной лимит
   - `set_monthly_limit(limit)` - установить месячный лимит
   - `check_limits()` - проверка превышения лимитов (возвращает предупреждение)

3. Расчет потребления:
   - Собирать данные от всех устройств типа "outlet"
   - Суммировать `energy_today` всех розеток
   - Для других устройств использовать приблизительные значения:
     - Лампа: 10-50 Вт в зависимости от яркости
     - Термостат: 100-500 Вт когда включен
     - Камера: 5-15 Вт когда включена

4. Сохранение статистики:
   - Ежедневно сохранять общее потребление в БД
   - Таблица `EnergyDaily` (date, total_consumption, device_breakdown)

### Шаг 10.2: Добавить модели БД для энергопотребления
**Файлы для изменения:**
- `src/database/models.py`

**Что сделать:**
- Создать модель `EnergyDaily`:
  - `id` (Primary Key)
  - `date` (Date) - дата
  - `total_consumption` (Float) - общее потребление (кВт·ч)
  - `device_breakdown` (String) - JSON с потреблением по устройствам
  - `created_at` (DateTime)

- Создать модель `EnergyLimit`:
  - `id` (Primary Key)
  - `limit_type` (String) - "daily" или "monthly"
  - `limit_value` (Float) - значение лимита
  - `updated_at` (DateTime)

### Шаг 10.3: Автоматический сбор данных
**Файлы для изменения:**
- `src/services/energy/energy_service.py`

**Что сделать:**
- Метод `_collect_energy_data()`:
  - Каждые 5 минут собирать данные от всех устройств
  - Сохранять в БД (таблица `EnergyReading` - опционально для детальной статистики)
  - В конце дня сохранять итоговую запись в `EnergyDaily`

- Запуск в отдельном потоке:
  ```python
  def start(self):
      self.thread = threading.Thread(target=self._run)
      self.thread.daemon = True
      self.thread.start()
  
  def _run(self):
      while self.running:
          self._collect_energy_data()
          time.sleep(300)  # Каждые 5 минут
  ```

### Шаг 10.4: Интегрировать EnergyService в HomeController
**Файлы для изменения:**
- `src/core/home_controller.py`

**Что сделать:**
- В `__init__` создать `self.energy_service = EnergyService(self)`
- В `start_system()` вызвать `self.energy_service.start()`
- Подписаться на события устройств для обновления статистики

### Шаг 10.5: Добавить GUI для отображения энергопотребления
**Файлы для создания:**
- `src/ui/widgets/energy_panel.py`

**Подробная реализация:**
1. Панель с:
   - Текущее потребление за сегодня (большими цифрами)
   - Прогресс-бар дневного лимита
   - Потребление за месяц
   - Топ-5 потребителей (список устройств с потреблением)
   - График потребления за последние 7 дней (matplotlib)

2. Настройки:
   - Установка дневного лимита
   - Установка месячного лимита
   - Предупреждение при превышении (чекбокс)

**Файлы для изменения:**
- `src/ui/gui_interface.py`:
  - Добавить вкладку "Энергопотребление"
  - Интегрировать `EnergyPanel`

**Тестирование этапа 10:**
- Проверить сбор данных от устройств
- Проверить расчет общего потребления
- Проверить сохранение в БД
- Проверить отображение в GUI и графики

---

## ЭТАП 11: Группировка устройств по комнатам
**Цель:** Организация устройств по комнатам для удобного управления

### Шаг 11.1: Создать сервис управления комнатами
**Файлы для создания:**
- `src/services/rooms/__init__.py` (пустой файл)
- `src/services/rooms/room_service.py`

**Подробная реализация:**
1. Создать класс `Room`:
   ```python
   class Room:
       def __init__(self, room_id, name, room_type):
           self.room_id = room_id
           self.name = name  # "Гостиная", "Спальня" и т.д.
           self.room_type = room_type  # "living", "bedroom", "kitchen", "bathroom"
           self.devices = []  # Список device_id
   ```

2. Создать класс `RoomService`:
   ```python
   class RoomService:
       def __init__(self, home_controller):
           self.controller = home_controller
           self.rooms = {}  # {room_id: Room}
           self._initialize_default_rooms()
   ```

3. Методы `RoomService`:
   - `add_room(room)` - добавить комнату
   - `remove_room(room_id)` - удалить комнату
   - `get_room(room_id)` - получить комнату
   - `get_all_rooms()` - получить все комнаты
   - `add_device_to_room(room_id, device_id)` - добавить устройство в комнату
   - `remove_device_from_room(room_id, device_id)` - удалить устройство из комнаты
   - `get_devices_in_room(room_id)` - получить устройства в комнате
   - `get_room_for_device(device_id)` - получить комнату устройства

4. Метод `_initialize_default_rooms()`:
   - Создать комнаты: "Гостиная", "Спальня", "Кухня", "Коридор"
   - Автоматически распределить существующие устройства по комнатам по названиям

### Шаг 11.2: Добавить модели БД для комнат
**Файлы для изменения:**
- `src/database/models.py`

**Что сделать:**
- Создать модель `Room`:
  - `id` (Primary Key)
  - `room_id` (String, Unique)
  - `name` (String)
  - `room_type` (String)
  - `created_at` (DateTime)

- Создать модель `RoomDevice` (связь многие-ко-многим):
  - `id` (Primary Key)
  - `room_id` (ForeignKey -> Room)
  - `device_id` (String)
  - `created_at` (DateTime)

### Шаг 11.3: Сохранение и загрузка комнат из БД
**Файлы для изменения:**
- `src/services/rooms/room_service.py`

**Что сделать:**
- При инициализации загружать комнаты из БД
- При добавлении/изменении сохранять в БД
- При удалении удалять из БД

### Шаг 11.4: Интегрировать RoomService в HomeController
**Файлы для изменения:**
- `src/core/home_controller.py`

**Что сделать:**
- В `__init__` создать `self.room_service = RoomService(self)`
- При добавлении нового устройства автоматически предлагать выбрать комнату

### Шаг 11.5: Обновить GUI для отображения по комнатам
**Файлы для изменения:**
- `src/ui/gui_interface.py`

**Подробная реализация:**
1. Изменить структуру главного окна:
   - Вкладки или боковое меню с комнатами
   - При выборе комнаты показывать только устройства этой комнаты
   - Кнопка "Все устройства" для просмотра всех

2. Панель управления комнатами:
   - Список комнат
   - Кнопки: "Добавить комнату", "Изменить", "Удалить"
   - Для каждой комнаты: список устройств с возможностью добавления/удаления

3. Виджет комнаты:
   - Название комнаты
   - Сетка устройств этой комнаты
   - Кнопка "Управление всей комнатой" (включить/выключить все устройства)

**Файлы для создания:**
- `src/ui/widgets/room_panel.py` - панель для отображения комнаты

**Тестирование этапа 11:**
- Создать комнаты и распределить устройства
- Проверить сохранение в БД
- Проверить отображение в GUI по комнатам
- Проверить управление устройствами через комнаты

---

## ЭТАП 12: Сервис статистики и аналитики
**Цель:** Сбор и анализ данных о работе системы

### Шаг 12.1: Создать сервис статистики
**Файлы для создания:**
- `src/services/statistics/__init__.py` (пустой файл)
- `src/services/statistics/statistics_service.py`

**Подробная реализация:**
1. Создать класс `StatisticsService`:
   ```python
   class StatisticsService:
       def __init__(self, home_controller):
           self.controller = home_controller
   ```

2. Методы для сбора статистики:
   - `get_device_usage_stats(device_id, period)` - статистика использования устройства:
     - Время работы (часы в день/неделю/месяц)
     - Количество включений/выключений
     - Среднее время работы за сессию
   
   - `get_system_stats(period)` - общая статистика системы:
     - Общее количество команд
     - Количество срабатываний автоматизаций
     - Количество уведомлений
     - Время работы системы
   
   - `get_room_stats(room_id, period)` - статистика по комнате:
     - Использование устройств в комнате
     - Энергопотребление комнаты
   
   - `get_trends(metric, period)` - тренды:
     - Изменение температуры за период
     - Изменение энергопотребления
     - Активность устройств

3. Расчет статистики:
   - Анализировать данные из `DeviceStateHistory`
   - Группировать по периодам (день, неделя, месяц)
   - Вычислять средние, максимумы, минимумы

### Шаг 12.2: Добавить модели БД для статистики
**Файлы для изменения:**
- `src/database/models.py`

**Что сделать:**
- Создать модель `DeviceStatistics` (опционально, для кэширования):
  - `id` (Primary Key)
  - `device_id` (String)
  - `period` (String) - "day", "week", "month"
  - `period_start` (DateTime)
  - `usage_hours` (Float)
  - `switch_count` (Integer)
  - `avg_session_duration` (Float)
  - `created_at` (DateTime)

### Шаг 12.3: Автоматический расчет статистики
**Файлы для изменения:**
- `src/services/statistics/statistics_service.py`

**Что сделать:**
- Метод `_calculate_daily_stats()`:
  - В конце каждого дня рассчитывать статистику за день
  - Сохранять в БД для быстрого доступа
  - Агрегировать в недельную/месячную статистику

- Запуск в отдельном потоке:
  ```python
  def start(self):
      self.thread = threading.Thread(target=self._run)
      self.thread.daemon = True
      self.thread.start()
  
  def _run(self):
      while self.running:
          # Проверка каждый час, расчет в полночь
          if datetime.now().hour == 0 and datetime.now().minute == 0:
              self._calculate_daily_stats()
          time.sleep(3600)  # Каждый час
  ```

### Шаг 12.4: Интегрировать StatisticsService в HomeController
**Файлы для изменения:**
- `src/core/home_controller.py`

**Что сделать:**
- В `__init__` создать `self.statistics_service = StatisticsService(self)`
- В `start_system()` вызвать `self.statistics_service.start()`

### Шаг 12.5: Добавить GUI для отображения статистики
**Файлы для создания:**
- `src/ui/widgets/statistics_panel.py`

**Подробная реализация:**
1. Панель с вкладками:
   - "Общая статистика": графики активности системы, тренды
   - "Устройства": статистика по каждому устройству
   - "Комнаты": статистика по комнатам
   - "Энергопотребление": графики потребления

2. Виджеты:
   - Выбор периода (день, неделя, месяц)
   - Графики (matplotlib):
     - График активности устройств
     - График времени работы
     - График количества команд
   - Таблицы с числовыми данными

3. Экспорт:
   - Кнопка "Экспорт в CSV"
   - Кнопка "Экспорт в JSON"

**Файлы для изменения:**
- `src/ui/gui_interface.py`:
  - Добавить вкладку "Статистика"
  - Интегрировать `StatisticsPanel`

**Тестирование этапа 12:**
- Проверить расчет статистики
- Проверить сохранение в БД
- Проверить отображение графиков в GUI
- Проверить экспорт данных

---

## ЭТАП 13: Экспорт данных и отчеты
**Цель:** Возможность экспорта данных для анализа

### Шаг 13.1: Создать сервис экспорта
**Файлы для создания:**
- `src/services/export/__init__.py` (пустой файл)
- `src/services/export/export_service.py`

**Подробная реализация:**
1. Создать класс `ExportService`:
   ```python
   class ExportService:
       def __init__(self, home_controller):
           self.controller = home_controller
   ```

2. Методы экспорта:
   - `export_device_history(device_id, start_date, end_date, format)`:
     - Экспорт истории состояний устройства
     - Форматы: CSV, JSON, Excel (опционально)
     - Поля: timestamp, state, temperature, brightness и т.д.
   
   - `export_energy_data(start_date, end_date, format)`:
     - Экспорт данных энергопотребления
     - Формат: CSV с колонками date, device_id, consumption
   
   - `export_statistics(period, format)`:
     - Экспорт статистики за период
     - Формат: JSON или CSV
   
   - `export_logs(level, start_date, end_date, format)`:
     - Экспорт логов системы
     - Фильтрация по уровню (INFO, WARNING, ERROR)
   
   - `export_full_report(start_date, end_date, format)`:
     - Полный отчет: все данные за период
     - Включает: устройства, энергопотребление, логи, статистику
     - Формат: JSON (структурированный) или HTML (читаемый)

3. Реализация экспорта в CSV:
   ```python
   import csv
   def _export_to_csv(data, filename):
       with open(filename, 'w', newline='') as f:
           writer = csv.writer(f)
           writer.writerow(data[0].keys())  # Заголовки
           for row in data:
               writer.writerow(row.values())
   ```

4. Реализация экспорта в JSON:
   ```python
   import json
   def _export_to_json(data, filename):
       with open(filename, 'w', encoding='utf-8') as f:
           json.dump(data, f, ensure_ascii=False, indent=2)
   ```

### Шаг 13.2: Добавить GUI для экспорта
**Файлы для изменения:**
- `src/ui/widgets/export_dialog.py`

**Подробная реализация:**
1. Диалог экспорта:
   - Выбор типа данных: "История устройств", "Энергопотребление", "Логи", "Полный отчет"
   - Выбор периода: начальная и конечная дата
   - Выбор формата: CSV, JSON, HTML (для отчета)
   - Поле для пути сохранения файла
   - Кнопки: "Экспорт", "Отмена"
   - Прогресс-бар при экспорте

2. Интеграция:
   - Кнопка "Экспорт" в панелях статистики, энергопотребления, логов
   - Меню "Файл -> Экспорт данных" в главном окне

**Файлы для изменения:**
- `src/ui/gui_interface.py`:
  - Добавить пункт меню "Экспорт"
  - Интегрировать `ExportDialog`

### Шаг 13.3: Генерация HTML отчетов
**Файлы для создания:**
- `src/services/export/report_generator.py`

**Подробная реализация:**
1. Создать класс `ReportGenerator`:
   - Метод `generate_html_report(data, template)`:
     - Генерация HTML отчета из данных
     - Использование шаблона (можно использовать Jinja2 или простую строковую замену)
     - Включение графиков (можно использовать matplotlib для генерации изображений)

2. Структура HTML отчета:
   - Заголовок с датами периода
   - Раздел "Общая информация": статистика системы
   - Раздел "Устройства": таблица со статистикой каждого устройства
   - Раздел "Энергопотребление": графики и таблицы
   - Раздел "События": важные события за период
   - Раздел "Логи": последние логи (опционально)

**Файлы для изменения:**
- `requirements.txt` - добавить `jinja2>=3.1.0` (опционально, для шаблонов)

**Тестирование этапа 13:**
- Экспортировать данные в разных форматах
- Проверить корректность экспортированных файлов
- Проверить генерацию HTML отчетов
- Проверить работу через GUI

---

## ЭТАП 14: Улучшения GUI для новых функций
**Цель:** Интеграция всех новых функций в удобный интерфейс

### Шаг 14.1: Создать главное меню с вкладками
**Файлы для изменения:**
- `src/ui/gui_interface.py`

**Что сделать:**
- Использовать `ttk.Notebook` для вкладок:
  - "Главная" - общий статус, быстрые действия
  - "Устройства" - все устройства (с фильтрацией по комнатам)
  - "Комнаты" - управление комнатами
  - "Расписания" - управление расписаниями
  - "Энергопотребление" - мониторинг энергии
  - "Статистика" - аналитика и графики
  - "Уведомления" - список уведомлений
  - "Настройки" - настройки системы

### Шаг 14.2: Добавить дашборд на главной вкладке
**Файлы для изменения:**
- `src/ui/gui_interface.py`

**Подробная реализация:**
1. Дашборд с виджетами:
   - Карточка "Статус системы": онлайн устройства, активные устройства
   - Карточка "Энергопотребление сегодня": текущее потребление, прогресс лимита
   - Карточка "Ближайшие расписания": список расписаний на сегодня
   - Карточка "Последние события": последние 5 событий/уведомлений
   - Мини-график температуры за последние 24 часа
   - Кнопки быстрых действий: "Утро", "Кино", "Сон"

2. Автообновление:
   - Обновлять дашборд каждые 5 секунд
   - Выделять важные события (тревоги, превышения лимитов)

### Шаг 14.3: Улучшить отображение устройств
**Файлы для изменения:**
- `src/ui/widgets/device_card.py`

**Что сделать:**
- Добавить цветовую индикацию:
  - Зеленый - устройство включено и онлайн
  - Красный - устройство выключено или офлайн
  - Желтый - предупреждение (например, низкий заряд батареи)
  
- Добавить иконки для типов устройств (можно использовать эмодзи или простые символы)
- Добавить контекстное меню (правая кнопка мыши):
  - "Настройки"
  - "История"
  - "Статистика"
  - "Удалить из комнаты"

### Шаг 14.4: Добавить уведомления в реальном времени
**Файлы для изменения:**
- `src/ui/gui_interface.py`

**Что сделать:**
- Всплывающие уведомления (toast notifications):
  - При критических событиях (дым, движение)
  - При превышении лимитов энергопотребления
  - При выполнении расписаний
  
- Реализация через `tkinter.messagebox` или кастомное окно
- Звуковые уведомления (опционально, через `winsound` на Windows или `os.system('say')` на macOS)

### Шаг 14.5: Добавить темную тему
**Файлы для создания:**
- `src/ui/themes/__init__.py` (пустой файл)
- `src/ui/themes/dark_theme.py`
- `src/ui/themes/light_theme.py`

**Подробная реализация:**
1. Создать классы тем:
   ```python
   class DarkTheme:
       bg_color = "#2b2b2b"
       fg_color = "#ffffff"
       button_bg = "#3c3c3c"
       # и т.д.
   ```

2. Применение темы:
   - В `GUIInterface` добавить метод `apply_theme(theme)`
   - Настройка в меню "Настройки -> Тема"
   - Сохранение выбранной темы в конфигурации

**Файлы для изменения:**
- `src/ui/gui_interface.py`:
  - Добавить переключение темы
  - Применить цвета ко всем виджетам

### Шаг 14.6: Добавить поиск и фильтрацию
**Файлы для изменения:**
- `src/ui/gui_interface.py`

**Что сделать:**
- Поле поиска в верхней панели:
  - Поиск устройств по названию
  - Фильтрация по типу устройства
  - Фильтрация по комнате
  - Фильтрация по статусу (вкл/выкл, онлайн/офлайн)

- Реализация:
  - Поле ввода с кнопкой "Поиск"
  - Выпадающий список для фильтров
  - Динамическое обновление списка устройств при вводе

**Тестирование этапа 14:**
- Проверить все вкладки и их функциональность
- Проверить дашборд и его автообновление
- Проверить уведомления
- Проверить поиск и фильтрацию
- Проверить переключение темы

---

## 📝 Дополнительные примечания

### Приоритеты новых этапов
- **Высокий приоритет:** Этапы 8, 9, 11 (новые устройства, расписания, комнаты)
- **Средний приоритет:** Этапы 10, 12 (энергопотребление, статистика)
- **Низкий приоритет:** Этапы 13, 14 (экспорт, улучшения GUI)

### Зависимости между этапами
- Этап 11 (комнаты) можно делать параллельно с этапом 8 (устройства)
- Этап 10 (энергопотребление) требует этап 8 (розетки)
- Этап 12 (статистика) использует данные из этапов 2, 10
- Этап 13 (экспорт) использует данные из всех предыдущих этапов

### Обновленный чеклист готовности

- [ ] Этап 1: Имитация устройств работает
- [ ] Этап 2: БД создается и данные сохраняются
- [ ] Этап 3: GUI запускается и отображает устройства
- [ ] Этап 4: Дополнительные функции GUI работают
- [ ] Этап 5: API отвечает на запросы
- [ ] Этап 6: Сценарии выполняются корректно
- [ ] Этап 7: Документация готова, проект протестирован
- [ ] Этап 8: Новые устройства работают и симулируются
- [ ] Этап 9: Расписания создаются и выполняются
- [ ] Этап 10: Энергопотребление отслеживается
- [ ] Этап 11: Устройства сгруппированы по комнатам
- [ ] Этап 12: Статистика рассчитывается и отображается
- [ ] Этап 13: Экспорт данных работает
- [ ] Этап 14: GUI улучшен и интегрирован

