### 📝 Шаблон Arrange-Act-Assert

**Ответ:** Шаблон Arrange-Act-Assert (AAA) — это способ структурирования юнит-тестов, который делит их на три четких этапа.
*   **Arrange (Подготовка):** На этом этапе вы подготавливаете все необходимое для теста: создаете экземпляры тестируемых классов, настраиваете начальные данные и создаете заглушки для зависимостей (моки и стабы).
*   **Act (Действие):** На этом этапе вы выполняете действие, которое хотите протестировать — вызываете один метод или функцию тестируемого кода.
*   **Assert (Проверка):** На этом этапе вы проверяете, соответствует ли результат выполнения метода вашим ожиданиям, используя утверждения (assertions).
- **Ответ из файла:** Шаблон Arrange-act-assert структурирует код теста в три секции: arrange, act, assert. Arrange – инициализация, Act – запуск, Assert – проверка.


### ✅ Преимущества Unit-тестирования

**Ответ:** Unit-тестирование предоставляет несколько ключевых преимуществ:
*   **Раннее обнаружение ошибок:** Позволяет находить и исправлять дефекты на самых ранних этапах разработки, что снижает их стоимость.
*   **Упрощение отладки:** Если тест не проходит, проблема, скорее всего, в только что написанном коде, что значительно сужает область поиска.
*   **Улучшение архитектуры кода:** Необходимость писать тестируемый код ведет к созданию более модульной, менее связанной архитектуры.
*   **Тесты как документация:** Набор тестов показывает, как должен использоваться код, служа живой документацией для разработчиков.
*   **Возможность рефакторинга:** Наличие тестового набора позволяет уверенно улучшать и изменять код, не опасаясь случайно сломать существующую функциональность.
- **Ответ из файла:** Тесты как документация, упрощение архитектуры приложения, возможность лучше разобраться в коде.


### 📊 Что такое тестовое покрытие

**Ответ:** Тестовое покрытие — это метрика, которая показывает, насколько полно тесты проверяют исходный код или требования.

**Основные виды покрытия кода:**
*   **Покрытие операторов (Statement Coverage):** Показывает, какие строки кода были выполнены во время тестов.
*   **Покрытие ветвей (Branch Coverage):** Проверяет, были ли протестированы все возможные пути выполнения (например, обе ветви `if-else`).
*   **Покрытие условий (Condition Coverage):** Требует, чтобы каждое булево выражение в условиях принимало значения `true` и `false`.
*   **Покрытие путей (Path Coverage):** Самый строгий вид, который учитывает все возможные уникальные маршруты выполнения кода.
Важно помнить, что высокий процент покрытия сам по себе **не гарантирует** отсутствия дефектов, но помогает выявить непроверенные участки кода.
- **Ответ из файла:** Тестовое покрытие – это одна из метрик оценки качества тестирования, представляющая из себя плотность покрытия тестами требований либо исполняемого кода.

### 🎭 Моки и стабы

**Ответ:** Моки и стабы — это виды тестовых дублеров (test doubles), которые используются для изоляции тестируемого кода от его зависимостей.

*   **Test Stub (Заглушка):** Используется для подмены реальной зависимости и возврата тестовых данных. Заглушка возвращает предопределенный результат и игнорирует данные, поступающие от тестируемого объекта.
*   **Mock Object (Мок-объект):** Частично реализует интерфейс, имеет предопределенное поведение и, что ключевое, **позволяет убедиться, что тестируемый код взаимодействует с ним ожидаемым образом**. Мок-объект проверяет, какие методы были вызваны, с какими параметрами и сколько раз.
  - **Ответ из файла:** 
  - test stub (заглушка) используется для получения данных из внешней зависимости, подменяя её.
  - mock object (мок-объект) частично реализует интерфейс и обеспечивает возможность убедиться, что вызовы mock объекта удовлетворяют спецификации.


### 🗓️ Планирование тестов

**Ответ:** Планирование тестов включает несколько ключевых принципов:
1.  **Основа на требованиях:** Тесты должны в первую очередь соответствовать не коду, а требованиям и спецификациям. На каждое требование должен быть как минимум один тест.
2.  **Использование формальных методик:** Для создания эффективных тестов следует применять техники тест-дизайна, такие как анализ граничных значений и классов эквивалентности.
3.  **Начать с позитивного сценария:** Рекомендуется начать с простого позитивного теста, который проверяет работу в штатном режиме.
4.  **Анализ покрытия кода:** Последнюю проверку полноты тестового набора следует проводить с помощью метрики «Code Coverage», которая наглядно показывает непротестированные участки кода.
- **Ответ из файла:** Тесты должны базироваться на спецификации, на каждое требование должен быть как минимум один тест, рекомендуется начать с простого позитивного теста.


### 📏 Правила организации тестов

**Ответ:** Вот основные правила организации модульных тестов:
*   **Читаемые имена:** Имя теста должно сообщать о тестируемом методе и проверяемом поведении (например, `TestIncorrectLogin`).
*   **Независимость:** Тесты не должны зависеть друг от друга и от порядка выполнения. Результат одного теста не должен влиять на другой.
*   **Скорость:** Юнит-тесты должны выполняться быстро.
*   **Одна ответственность:** Каждый тест должен быть настроен на выявление одной возможной ошибки.
*   **Отсутствие дублирования:** Тесты не должны дублировать друг друга.
*   **Изоляция:** Тестируемый код не должен работать с сетью, файлами или базой данных.
- **Ответ из файла:** Тесты не должны зависеть друг от друга, каждый тестовый случай должен быть настроен на выявление лишь одной возможной ошибки, тесты не должны дублировать друг друга.


### 🎯 Unit-тестирование: белый или черный ящик?

**Ответ:** **Unit-тестирование является классическим примером тестирования методом белого ящика (White-Box Testing)**.

Это означает, что разработчик, пишущий тесты, имеет полный доступ к исходному коду и знает его внутреннюю структуру, что позволяет тестировать не только внешнее поведение, но и внутренние ветвления, циклы и пути выполнения.

### 🔍 Тестирование программы методом белого ящика

**Ответ:** Чтобы провести тестирование вашей программы методом белого ящика, выполните следующие шаги:
1.  **Анализ кода:** Тщательно изучите внутреннюю структуру кода, который необходимо протестировать. Поймите его логику, все возможные ветвления (условия `if-else`) и циклы.
2.  **Определение тестовых сценариев:** Основываясь на структуре кода, определите сценарии, которые обеспечат максимальное покрытие. Создайте тесты для:
    *   Всех логических ветвей (обеспечивая покрытие ветвей).
    *   Граничных значений для условий в циклах и операторах `if`.
    *   Разных комбинаций условий (покрытие условий).
3.  **Написание и выполнение тестов:** Используя шаблон AAA и фреймворк для тестирования (например, unittest в Python), напишите и запустите тесты.
4.  **Измерение покрытия:** Воспользуйтесь инструментами для анализа покрытия кода (например, `coverage.py` для Python), чтобы оценить, какие части кода были выполнены во время тестов, и выявить непроверенные участки.
